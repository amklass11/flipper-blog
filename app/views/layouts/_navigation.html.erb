<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    
      <%= link_to "flipper Blog", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Articles", articles_path %></li>
        <li><%= link_to "Create Article", new_article_path %></li>

      
        <% if logged_in? %>
          <li><%= link_to "Log out", logout_path, method: :delete %></li>
<% else %>
  <li><%= link_to "Login", login_path, method: :create %></li>
<% end %>
<%= form_with url: search_articles_path, method: :post do |form| %>
  <%= form.search_field :title_search, placeholder: "Search" %>
  <%= form.submit "Search", oninput: 'this.form.requestSubmit' %>
<% end %>
      </ul>

        
        >
        
           </div>
  </div>
</nav>